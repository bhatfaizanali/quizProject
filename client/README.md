# Client

This is a minimal Create React App scaffold.

Quick start

1. cd client
2. npm install
3. npm start

The dev server proxies API requests to `http://localhost:4000` (see `package.json` `proxy`). The current `App` fetches `/api/quizzes` and lists published quizzes.
